This test verifies that adding and reordering fields work correctly.

change1.go contains a struct, and change2.go is a different version of that
struct with fields added and reordered.

run.sh encodes a value of the first struct, then decodes it into the second.
